import 'package:flutter/material.dart';

class AmountPayScreen extends StatelessWidget {
  const AmountPayScreen({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Hello"),
    );
  }
}